{
  "name": "blogzone-mern-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix backend",
    "backend": "npm run server --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  }
}
